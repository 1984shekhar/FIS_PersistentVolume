spec:
  template:
    spec:
      containers:
        - 
          resources:
            requests:
              cpu: "0.2"
#              memory: 256Mi
            limits:
              cpu: "1.0"
#              memory: 256Mi
        - volumeMounts:
          - name: vol0001
            mountPath: /home/cpandey/pv0001_mountpath
      volumes:
        - name: vol0001
          persistentVolumeClaim:
            claimName: pvc0001
